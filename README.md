# Esports Pick & Ban Controller для vMix

Это веб-приложение для управления фазой пиков и банов (Pick & Ban) в киберспортивных трансляциях. Оно позволяет синхронизировать красивый веб-оверлей (для OBS/vMix) с аппаратным управлением vMix (запуск видео-перебивок, плашек и обновление титров).

---

## 🛠 1. Необходимый софт

Для работы приложения на компьютере должны быть установлены:

1.  **Node.js** (версия 18 или выше).
    *   Это среда выполнения для веб-сервера.
    *   👉 **[Скачать Node.js (LTS версию)](https://nodejs.org/ru)**.
2.  **vMix** (версия 24 или выше, лицензия HD, 4K или Pro).
    *   Необходим для приема команд через Web API.
    *   👉 **[Сайт vMix](https://www.vmix.com/)**.

---

## 🚀 2. Установка и Запуск

1.  Распакуйте архив с приложением в удобную папку.
2.  Найдите файл **`start_app.bat`** в корневой папке.
3.  Запустите его двойным кликом.
    *   Откроются два черных окна консоли. **Не закрывайте их!** Это сервер (Backend) и клиент (Frontend).
    *   При первом запуске установка может занять 1-2 минуты (скачиваются библиотеки).
4.  После запуска автоматически откроется браузер по адресу `http://localhost:5173`.

---

## 📺 3. Настройка vMix

Чтобы приложение могло управлять vMix, нужно правильно настроить проект (Preset).

### Шаг А: Включение Web Controller
1.  Откройте vMix.
2.  Зайдите в **Settings (Настройки)** -> **Web Controller**.
3.  Убедитесь, что галочка **Enable** стоит.
4.  Запомните **Port** (обычно 8088).
5.  Если vMix и приложение на разных ПК, запомните IP-адрес этого компьютера (указан в строке Website Address, например `192.168.1.50`).

### Шаг Б: Добавление необходимых Инпутов (Inputs)
Приложение ищет инпуты по **конкретным именам**. Важно назвать их или файлы именно так:

1.  **Титр для имен карт:**
    *   Добавьте GT Title или XAML титр.
    *   Назовите этот инпут (Input Name): **`PIC_BAN.gtzip`** (или измените константу в коде, если умеете).
    *   Внутри титра должны быть текстовые поля `TextBlock1`, `TextBlock2`... соответствующие номерам шагов пик-бана.

2.  **Видео карт:**
    *   Добавьте видео-файлы карт (например, `Dust2.mp4`, `Mirage.mp4`, `Inferno.mp4`).
    *   **ВАЖНО:** Имя инпута в vMix должно совпадать с именем карты в приложении + `.mp4`.
    *   *Пример:* Если в приложении карта называется "Ancient", видео-инпут должен называться `Ancient.mp4`.

3.  **Плашки статусов (Overlay 3):**
    *   Добавьте 3 картинки (Image Input) для подложек под текст.
    *   Обязательные имена файлов/инпутов:
        *   `plaska_BAN small.png` (Для банов)
        *   `plaska_PICK_small.png` (Для пиков)
        *   `plaska_TB_small.png` (Для решающей карты / Decider)

### Как работает логика в vMix (автоматически):
Когда вы нажимаете **GO** в приложении:
1.  Видео карты (напр. `Mirage.mp4`) запускается на **Overlay 2**.
2.  Картинка статуса (напр. `plaska_PICK_small.png`) запускается на **Overlay 3**.
3.  Через 4 секунды (настраивается) оба Оверлея выключаются.

---

## 🎛 4. Работа с Веб-интерфейсом

### Главный экран (Админка)
*   **Team A / Team B:** Поля для ввода названий команд.
*   **Список шагов:** Кликните на шаг, чтобы выбрать карту.
*   **Кнопка GO:** Запускает сценарий в vMix и обновляет веб-оверлей.
*   **Reset All:** Сброс всего матча.
*   **+ Add Map Images:** Загрузка картинок карт для отображения в админке (не влияет на vMix, только для красоты интерфейса).

### Настройки (Settings Panel)
Нажмите кнопку **⚙ Settings** в верхнем меню.

#### 📡 Automations & Connection (Связь)
*   **vMix LAN Connection:**
    *   **IP Address:** Введите IP компьютера, где запущен vMix (если на том же ПК — оставьте `127.0.0.1`).
    *   **Port:** Порт веб-контроллера vMix (по умолчанию `8088`).
*   **vMix Video -> Overlay Delay:**
    *   Время задержки (в секундах) между нажатием кнопки GO (старт видео в vMix) и появлением карточки на веб-оверлее. Нужно подбирать под длительность вашей анимации появления.

#### 🎨 Theme Colors (Цвета)
Здесь настраиваются градиенты для плашек.
*   **Ban Gradient Start/End:** Цвета для этапа БАН (обычно красные).
*   **Pick Gradient Start/End:** Цвета для этапа ПИК (обычно зеленые).
*   **Decider Gradient:** Цвета для десайдера (желтые/нейтральные).
*   *Совет:* Ползунок справа от выбора цвета регулирует прозрачность (Opacity).

#### 📐 Geometry / Layout (Размеры и отступы)
Настройка внешнего вида веб-оверлея (того, что идет в эфир).
*   **Global Scale:** Общий масштаб всего оверлея (Zoom).
*   **Item Scale:** Размер отдельной плашки карты.
*   **Vertical Gap:** Расстояние между строками карт.
*   **Horizontal Center Offset:** Насколько сильно колонки команд раздвинуты от центра.
*   **Vertical Top Offset:** Отступ всего списка от верхнего края экрана.
*   **Decider Position:** Тонкая настройка положения последней карты (Десайдера) по осям X и Y.

#### 🔤 Typography & Locale (Текст)
*   **Use Russian Language:** Переключает надписи на плашках (PICK -> ПИК, BAN -> БАН).
*   **Font Size:** Размер шрифта названий карт.
*   **Select Font:** Выбор шрифта. Можно загрузить свой файл `.ttf` или `.otf` через кнопку "Upload Font".

---

## 🌐 5. Как вывести в OBS / vMix

Чтобы зрители видели график пиков-банов:

1.  В приложении нажмите кнопку **OPEN OVERLAY** (справа сверху).
2.  Скопируйте адрес из адресной строки (обычно `http://localhost:5173/overlay`).
3.  **В OBS:**
    *   Добавьте источник "Browser" (Браузер).
    *   Вставьте ссылку.
    *   Установите разрешение 1920x1080.
4.  **В vMix:**
    *   Add Input -> Web Browser.
    *   Вставьте ссылку.

Теперь, когда вы управляете процессом в админке, этот браузерный источник будет обновляться автоматически.

---

## ⚠️ Решение проблем

**1. Нажимаю GO, но в vMix ничего не происходит.**
*   Проверьте, включен ли Web Controller в vMix.
*   Проверьте IP и Порт в настройках приложения.
*   Проверьте консоль логов (кнопка `Logs` внизу приложения) — там будет написано, какая ошибка (например, "Function not found" или "Connection refused").

**2. Видео не запускается, ошибка в логах.**
*   Убедитесь, что имя инпута видео в vMix ТОЧНО совпадает с именем карты. (Пример: Карта "Nuke", инпут "Nuke.mp4").

**3. Оверлей не обновляется на другом компьютере.**
*   Убедитесь, что оба компьютера в одной сети.
*   В OBS на втором компьютере используйте IP-адрес сервера вместо `localhost` (например: `http://192.168.1.5:5173/overlay`).
*   Если не грузится — отключите Брандмауэр Windows (Firewall) на компьютере с сервером.
